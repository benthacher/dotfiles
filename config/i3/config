# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $alt Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
# exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# Use pactl to adjust volume in Pulse1.
# set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id $HOME/.config/i3/i3-volume-ctrl/volume-increase.sh
bindsym XF86AudioLowerVolume exec --no-startup-id $HOME/.config/i3/i3-volume-ctrl/volume-decrease.sh
bindsym XF86AudioMute exec --no-startup-id $HOME/.config/i3/i3-volume-ctrl/volume-mute.sh
# bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle

# Bind media controls
bindsym XF86AudioPlay exec --no-startup-id playerctl play-pause
bindsym XF86AudioNext exec --no-startup-id playerctl next
bindsym XF86AudioPrev exec --no-startup-id playerctl previous

bindsym XF86Calculator exec --no-startup-id i3-sensible-terminal -e "bc -l"

# Use brightnessctl to set brightness with function keys
bindsym XF86MonBrightnessUp exec --no-startup-id ~/.config/polybar/scripts/brightness.sh up
bindsym XF86MonBrightnessDown exec --no-startup-id ~/.config/polybar/scripts/brightness.sh down

# Set brightness through rofi dmenu window
bindsym $mod+b exec --no-startup-id dunstify "$(brightnessctl s $(rofi -dmenu -p 'Set Brightness' -l 0 -cols 10))"

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# Screenshot
bindsym Print exec --no-startup-id flameshot gui

# start a terminal
bindsym $mod+Return exec --no-startup-id i3-sensible-terminal
bindsym $mod+KP_Enter exec --no-startup-id i3-sensible-terminal

# kill focused window
bindsym $mod+Shift+q kill

# start custom drun
bindsym $mod+d exec --no-startup-id ~/.config/polybar/scripts/menu
bindsym XF86Search exec --no-startup-id ~/.config/polybar/scripts/menu

# run single command
bindsym $mod+r exec --no-startup-id ~/.config/i3/run_command_dialog.sh

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
# NO: this stinks, tabbed and stacked are garbo, toggle split is where it's at
# bindsym $mod+s layout stacking
# bindsym $mod+w layout tabbed
# bindsym $mod+e layout toggle split

bindsym $mod+g exec --no-startup-id ~/.config/i3/toggle_gaps.sh

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle
bindsym --whole-window $mod+button2 floating toggle

# change focus between tiling / floating windows
bindsym $alt+Tab focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 exec --no-startup-id /home/ben/.config/i3/ws $ws1
bindsym $mod+Shift+2 exec --no-startup-id /home/ben/.config/i3/ws $ws2
bindsym $mod+Shift+3 exec --no-startup-id /home/ben/.config/i3/ws $ws3
bindsym $mod+Shift+4 exec --no-startup-id /home/ben/.config/i3/ws $ws4
bindsym $mod+Shift+5 exec --no-startup-id /home/ben/.config/i3/ws $ws5
bindsym $mod+Shift+6 exec --no-startup-id /home/ben/.config/i3/ws $ws6
bindsym $mod+Shift+7 exec --no-startup-id /home/ben/.config/i3/ws $ws7
bindsym $mod+Shift+8 exec --no-startup-id /home/ben/.config/i3/ws $ws8
bindsym $mod+Shift+9 exec --no-startup-id /home/ben/.config/i3/ws $ws9
bindsym $mod+Shift+0 exec --no-startup-id /home/ben/.config/i3/ws $ws10

bindsym $mod+Shift+minus move scratchpad
bindsym $mod+Shift+plus scratchpad show

bindsym ctrl+$mod+Right exec --no-startup-id ~/.config/i3/next
bindsym ctrl+$mod+Left exec --no-startup-id ~/.config/i3/previous

bindsym $mod+Tab exec --no-startup-id rofi -modi window -show window

# reload the configuration file
# bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
# bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

bindsym ctrl+$alt+Delete exec --no-startup-id i3-sensible-terminal -e "(cat /home/ben/.cache/wal/sequences &) && i3 floating enable && htop"
bindsym --release $mod+Shift+k exec --no-startup-id xkill

# run wallpaper
bindsym $mod+Shift+p exec --no-startup-id ~/Projects/scripts/wallpaper
bindsym $mod+Shift+l exec --no-startup-id ~/Projects/scripts/wallpaper -l

# run my amazing, super cool, hack of sxiv to execute wallpaper when the user selects it
bindsym $mod+Shift+ctrl+p exec --no-startup-id ~/Projects/scripts/wallselect
bindsym $mod+Shift+ctrl+l exec --no-startup-id ~/Projects/scripts/wallselect -l

# Search spotify
bindsym $mod+Shift+s exec --no-startup-id spotify --uri=spotify:search:"$(rofi -dmenu -i -p 'Search Spotify:')"

# Set course
bindsym $mod+Shift+c exec --no-startup-id /home/ben/Projects/scripts/course-menu

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 1 px or 1 ppt
        bindsym Down resize shrink height 1 px or 1 ppt
        bindsym Up resize grow height 1 px or 1 ppt
        bindsym Right resize grow width 1 px or 1 ppt

        # same bindings, but for the arrow keys
        bindsym Control+Left resize shrink width 5 px or 5 ppt
        bindsym Control+Down resize shrink height 5 px or 5 ppt
        bindsym Control+Up resize grow height 5 px or 5 ppt
        bindsym Control+Right resize grow width 5 px or 5 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

for_window [class="Sxiv Wallpaper"] floating enable

# make eagle schematic/board windows float
for_window [class="Eagle" title="brd"] floating enable
for_window [class="Eagle" title="brd"] resize set width 1424
for_window [class="Eagle" title="brd"] resize set height 970

for_window [class="Eagle" title="sch"] floating enable
for_window [class="Eagle" title="sch"] resize set width 1424
for_window [class="Eagle" title="sch"] resize set height 970

# make kicad schematic/board windows float
for_window [class="KiCad" title="Footprint Library Browser"] floating enable
for_window [class="KiCad" title="3D Viewer"] floating enable

# make zoom dialogs floating
for_window [class="zoom"] floating enable
for_window [class="zoom" title="Zoom Meeting"] floating disable
for_window [class="zoom" title="Zoom - Licensed Account"] floating disable

# zoom launched from browser is different for some dumb reason
for_window [class="join\?action"] floating enable
for_window [class="join\?action" title="Zoom Meeting"] floating disable
for_window [class="join\?action" title="Zoom - Licensed Account"] floating disable

# zoom launched from browser is different for some dumb reason
for_window [class="start\?action"] floating enable
for_window [class="start\?action" title="Zoom Meeting"] floating disable
for_window [class="start\?action" title="Zoom - Licensed Account"] floating disable

# JLink ui popup
for_window [class="JLinkGUIServerExe"] floating enable

bindsym $mod+ctrl+r mode "resize"

gaps inner 20
gaps outer 10

default_border none
default_floating_border none
for_window [class="^.*"] border pixel 0

for_window [urgent="latest"] focus

# # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # #
#                            STARTUP
# # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # #

# Run reload script always, but this shouldn't run the first time
exec_always --no-startup-id "~/.config/i3/reload.sh"

# Run login script once
exec --no-startup-id "~/.config/i3/login.sh 2>&1 | tee -a ~/.config/i3/login-log.txt"

# parcellite bindings bc i forget them a lot
# ctrl+alt+H : history
# ctrl+alt+P : preferences
